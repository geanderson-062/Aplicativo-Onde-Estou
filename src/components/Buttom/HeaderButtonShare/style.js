import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  button: {
    alignItems: "center",
    backgroundColor: "#fff",
    borderColor: "#000",
    borderRadius: 100,
    borderWidth: 2,
    padding: 5,
  },
});

export default styles;
