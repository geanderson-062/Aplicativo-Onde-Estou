import { StyleSheet } from "react-native";

// css style

const styles = StyleSheet.create({
  button: {
    alignItems: "center",
    backgroundColor: "#fff",
    borderColor: "#000",
    borderRadius: 100,
    borderWidth: 2,
    padding: 10,
    marginBottom: "10%",
    width: "70%",
  },
  buttonText: {
    fontSize: 20,
    color: "black",
  },
});

export default styles;
